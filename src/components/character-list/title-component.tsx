import {
  Box,
  Typography
} from "@mui/material";

const Title = () => {
  return (
    <Box sx={{ textAlign: "center" }}>
      <Typography variant="h4">Characters</Typography>
    </Box>
  );
};

export default Title;
